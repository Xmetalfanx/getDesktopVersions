 
function getArchVersion()
{
    archVersion=$(curl -s "https://www.archlinux.org/packages/community/x86_64/$1/" | grep "<h2>$1" | sed 's/<h2>//g;s/<\/h2>//g;s/^[ ]*//g')
}

function displayArchVersion()
{
    echo -e  "Arch Version of $1: $archVersion"
    userPrompt
}